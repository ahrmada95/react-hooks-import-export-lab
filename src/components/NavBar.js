import React from "react";

function NavBar() {
  return (
    <nav>
      <a href="#home">I'm a link!</a>
    </nav>
  );
}

export default NavBar
// export default NavBar //this will get overrided if you have ANY other export!
// export { x } //another way to do export, you can do it one at a time or compounded